# Copy this file to `.env` and fill in your real Supabase credentials.
# Do NOT commit your real `.env` to source control.

# URL for your Supabase project (example: https://xyzcompany.supabase.co)
SUPABASE_URL=https://your-project.supabase.co

# Public (anon) key used by the frontend and most server flows
SUPABASE_ANON_KEY=your-anon-key-here

# Service role (secret) key used for privileged admin actions
# Keep this secret and only use it on trusted server environments
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Optional: frontend environment names (if used)
VITE_SUPABASE_URL=${SUPABASE_URL}
VITE_SUPABASE_ANON_KEY=${SUPABASE_ANON_KEY}
VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key
